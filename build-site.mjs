import{cp,mkdir,rm}from'node:fs/promises';import{join}from'node:path';const root=process.cwd(),out=join(root,'_site'),files=['index.html','styles.css','app.js','analyzer.js','catalog-client.js','catalog'];await rm(out,{recursive:true,force:true});await mkdir(out,{recursive:true});for(const file of files)await cp(join(root,file),join(out,file),{recursive:true});console.log('Static site ready in _site');
